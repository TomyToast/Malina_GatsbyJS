import styled from 'styled-components';

const Info = styled.div`
    padding: 5rem;
    font-size: 1.5rem;
    font-family: serif;
    color: ${({ theme }) => theme.colors.white};
    text-align: center;
`;

export default Info;